les tests statistique

L'orsque le pr > 0,05 nous retenons l'hypothese 0
l'orsque le pr < 0,05 nous retenons l'hypothese 1

1 T test (t.test(data = predata, GAD_7_Score~Gender))
avoir seulement deux groupes
variable a predire numerique
h0: il n'y a pas de difference significative entre les deux groupes: moyenne(Homme) = moyenne(femme) pvalue > 0,05
h1: il y a une difference significative entre les deux groupes: moyenne(Homme) different de  moyenne(femme) pvalue < 0,05


2 Anova (modelanova <- aov(data = predata, GAD_7_Score~Dominant_Content_Type))
avoir plus de deux variables
variable a predire en numerique
h0: Il n’existe pas de différence significative du score d’anxiété (GAD-7) selon le type de contenu dominant consommé.
h1: Le score d’anxiété (GAD-7) diffère significativement selon le type de contenu dominant consommé.

3 Anova (modelanova1 <- aov(data = predata, GAD_7_Score~User_Archetype))
h0: il n'existe pas de difference significative du score d'anxiete selon l'Utilisateur archétype
h1: il existe une difference significative du score d'anxiete selon l'Utilisateur archétype


4 Anova (modelanova2 <- aov(data = predata, GAD_7_Score~Primary_Platform))
h0: il n'existe pas de difference significative du score d'anxiete selon le type d'utilisateur
h1: il existe une difference significative du score d'anxiete selon le type d'utilisateur

5 Anova (modelanova3 <- aov(data = predata, GAD_7_Score~Activity_Type))
h0: il n'existe pas de difference significative du score d'anxiete selon le type d'activite
h1: il existe une difference significative du score d'anxiete selon le type d'activite


pour Les annalise des variables numeriques on efectuera des test de corelation

la standardisation
c'est d'empecher qu'une valeur trop haute influence le 
dimunier l'impacte d'une variable qui a un grand impacte sur une autre 

